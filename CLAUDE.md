# VibeTrip — Claude 工作規範

## 每次執行完必須回報

每次完成任何修改後，結尾必須列出：
- **改了哪些檔案**（附路徑）
- **每個檔案改了什麼**（一句話說明）

格式範例：
```
**變更摘要**
- `src/navigation/TabNavigator.jsx` — 文字 label 換成 Ionicons icon，Profile/Trip tab 加 reset 邏輯
- `src/screens/AR/services/transportApi.js` — 修正 API 參數名稱與回應欄位對應
```
